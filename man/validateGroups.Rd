% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MissingModel.R
\name{validateGroups}
\alias{validateGroups}
\title{Validate the sample-to-group mapping and contrast matrix}
\usage{
validateGroups(values, groups, comparisons = NULL)
}
\arguments{
\item{values}{A numeric matrix or data.frame of log2 intensities, samples in columns.}

\item{groups}{A factor vector specifying the group assignment for each sample.}

\item{comparisons}{A contrast matrix, generated by `limma::makeContrasts`.
`NULL` for callers such as `plotPost()` that name a single contrast
directly, in which case only `values` and `groups` are checked.}
}
\value{
`groups`, coerced to a factor.
}
\description{
Checks the inputs that the model silently depended on before: that `groups`
labels every column of `values`, that at least two groups are actually
populated, and that every group named by a contrast exists in `groups`.
}
\keyword{internal}
