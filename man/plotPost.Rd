% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPost.R
\name{plotPost}
\alias{plotPost}
\title{Plot Posterior Distribution}
\usage{
plotPost(
  values,
  id,
  groups,
  contrast,
  threshold = 0,
  cenTend = c("median", "mode", "mean")[1],
  compVal = 0,
  ROPE = c(-0.2, 0.2),
  n.adapt = 500,
  burn.in = 500,
  n.iter = 5000,
  n.chains = 2,
  credMass = 0.95,
  HDItextPlace = 0.7,
  xlab = NULL,
  xlim = NULL,
  yaxt = NULL,
  ylab = NULL,
  main = NULL,
  cex = NULL,
  cex.lab = NULL,
  col = NULL,
  border = NULL,
  showCurve = FALSE,
  breaks = NULL,
  ...
)
}
\arguments{
\item{values}{A numeric matrix or data.frame of log2 intensities. Rows correspond to proteins
(with protein IDs as rownames), and columns correspond to samples. Each row must
contain at least one non-NA value.}

\item{id}{A character string specifying the protein ID to plot.}

\item{groups}{A factor vector specifying the group assignment for each sample.}

\item{contrast}{A string specifying which two groups are compared.}

\item{threshold}{Numeric. The missing proportion threshold that determines
which missingness mechanism model to use. If the protein's missingness is < `threshold`,
the logistic model is used; otherwise, the truncated normal model is used.}

\item{cenTend}{Character string. The statistic to represent central tendency.}

\item{compVal}{An optional value for comparison (e.g. a null value).}

\item{ROPE}{A numeric vector of length 2 specifying the lower and upper bounds of the
Region of Practical Equivalence (ROPE). Values within this interval are considered
practically equivalent to the null value (e.g., `c(-0.1, 0.1)` for log fold change).}

\item{n.adapt}{Integer. Number of iterations for the adaptive phase.}

\item{burn.in}{An integer denoting number of burn-in steps.}

\item{n.iter}{Integer. Number of iterations after burn-in.}

\item{n.chains}{Integer. Number of MCMC chains to run.}

\item{credMass}{A numeric value between 0 and 1 specifying the mass of the
credible interval to compute.}

\item{HDItextPlace}{HDItextPlace.}

\item{xlab}{xlab}

\item{xlim}{xlim}

\item{yaxt}{yaxt}

\item{ylab}{ylab}

\item{main}{main}

\item{cex}{cex}

\item{cex.lab}{cex.lab}

\item{col}{col}

\item{border}{border}

\item{showCurve}{showCurve}

\item{breaks}{breaks}

\item{...}{Additional arguments passed to control plotting.}
}
\value{
A posterior distribution plot.
}
\description{
Creates a plot of posterior distribution with HDI and other statistics.
}
\details{
This function is modified from the \code{plotPost} function written by John K. Kruschke for his book
"Doing Bayesian Data Analysis" (2nd edition).
Modifications include: Users can directly input the data, protein ID, and other information to get the posterior distribution.
}
\examples{

# Assuming 'my_data' is a matrix and 'groups' is a factor
my_data <- matrix(rnorm(1200), nrow = 300, ncol = 4)
rownames(my_data) <- paste0("Protein_", 1:300)
colnames(my_data) <- paste0("Sample_", 1:4)
groups_factor <- factor(c("Control", "Control", "Treatment", "Treatment"))

plotPost(
  values = my_data,
  id = "Protein_1",
  groups = groups_factor,
  contrast = "Treatment - Control",
  compVal = 0,
  ROPE = c(-0.1, 0.1)
)

}
\references{
Kruschke, J. K. (2015). Doing Bayesian Data Analysis:
A Tutorial with R, JAGS, and Stan (2nd ed.). Academic Press.
}
\author{
John K. Kruschke (original `plotPost` function), Mengchun Li (port and integration with `missBayes`)
}
